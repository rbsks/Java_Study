1. Spring web mvc + jdbc + oracle + mybatis

2. lib 추가 -> mvn사용. spring, spring-jdbc, oracle, mybatis, mybatis-spring

3. application run 환경설정
	- servlet에서는 web.xml
	- spring에서는 root-context.xml or servlet-context.xml
4. java domain, entity, dto, bean
	EmpInfo

5. java service	(interface)
	dao와 controller 사이
	EmpService
		dao가 가지고있는 메소드를 호출할 수 있는 메소드를 만듦.
	EmpServiceImpl
		service에서 만든 메소드를 구현
		
6. java dao
	interface -> EmpDao(interface)
	emp table data sql

7. xml mapper
	EmpDaoMapper.xml
	실제 쿼리문
	
8. java controller
	EmpInfo의 data를 받고 이것을 jsp에 보내는 작업